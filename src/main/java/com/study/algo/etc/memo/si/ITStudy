# ASIS
- 현재의 업무 프로세스에 대한 분석

# TOBE
- 미래에 개선될 업무 프로세스에 대한 분석

# HashSet, TreeSet, LinkedHashSet
 1) HashSet
    - 순서 없이 저장
 2) TreeSet
 	- 값에 따라서 순서대로 저장 (데이터가 담기면서 정렬됨)
 	- 정렬된 값을 필요로 한다면 해당 클래스 사용
 3) LinkedHashSet
 	- 저장된 순서에 따라 순서가 결정
 	- 저장 순서를 유지해야 하면 해당 클래스 사용
 >>> 성능은 HashSet, LinkedHashSet > TreeSet이지만 용도에 따라 사용하면 됨
 
 